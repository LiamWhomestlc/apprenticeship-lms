tsx "use client"; import { useState } from "react"; export default function MonthlyReviewForm({learnerId}:{learnerId:string}){ const [notes,setNotes] = useState(""); const [loading,setLoading] = useState(false); return (<form onSubmit={async e=>{ e.preventDefault(); setLoading(true); await fetch("/api/reviews",{method:"POST",body:JSON.stringify({learnerId,periodStart:new Date().toISOString(),periodEnd:new Date().toISOString(),assessorNotes:notes})}); setLoading(false); setNotes(""); alert("Review submitted"); }} className="space-y-4"> <textarea value={notes} onChange={e=>setNotes(e.target.value)} className="w-full border rounded-xl p-3" placeholder="Assessor notes"/> <button disabled={loading} className="px-4 py-2 rounded-xl bg-black text-white">{loading?"Saving...":"Save review"}</button> </form>); }

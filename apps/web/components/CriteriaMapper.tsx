tsx "use client"; import { useState,useEffect } from "react"; type Criterion={id:string;code:string;description:string}; export default function CriteriaMapper({criterionOptions,onChange}:{criterionOptions:Criterion[];onChange:(ids:string[])=>void;}){ const [selected,setSelected]=useState<string[]>([]); useEffect(()=>onChange(selected),[selected]); return (<div className="space-y-2">{criterionOptions.map(c=>(<label key={c.id} className="flex items-start gap-2"><input type="checkbox" onChange={e=>setSelected(prev=>e.target.checked?[...prev,c.id]:prev.filter(id=>id!==c.id))}/><span><b>{c.code}</b> – {c.description}</span></label>))}</div>); }
